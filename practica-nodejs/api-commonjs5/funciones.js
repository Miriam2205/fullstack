const suma = ( a , b ) => a + b
const resta = ( a , b ) => a - b
const multiplicacion = ( a , b ) => a * b
const division = ( a , b ) => a / b
const potencia = ( a , b ) => a ** b
const raiz = ( a , b ) => a ** ( 1 / b )
module.exports={
    suma,
    resta,
    multiplicacion,
    division,
    potencia,
    raiz
}