const sumar    = ( a , b ) => a + b 
const restar   = ( a , b ) => a - b 
const multi    = ( a , b ) => a * b 
const division = ( a , b ) => a / b 

module.exports = {
    sumar,
    restar,
    multi,
    division
}